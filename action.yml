name: 'template-transformer-and-cleanup'
author: 'thynclabs'
description: 'ztodo'

inputs:
  text:
    description: 'Text to replace'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.text }}
branding:
  icon: terminal
  color: green
